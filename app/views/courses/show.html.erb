<h1><%= @course.name %></h1>
<ul>
  <h2>Students enrolled in <%= @course.name %> in order from highest grade to lowest grade: </h2>
  <% @students.each do |student| %>
  <li><%= student.name %></li>
  <% end %>
</ul>
